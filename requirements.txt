# Chronos Forensics Engine – core dependencies
# Python >= 3.10 recommended (zoneinfo is in stdlib)

# Core pipeline dependencies
pyyaml>=6.0
pytest>=7.0.0

# Optional – only needed for real binary EVTX files
python-evtx>=0.7.4

# Optional – PDF report generation
fpdf2>=2.7.0
